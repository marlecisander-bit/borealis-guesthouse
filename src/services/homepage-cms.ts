import 'server-only';import {createClient} from '@supabase/supabase-js';import {createServerSupabaseClient} from '@/lib/supabase/server';import {getAdminSession} from '@/lib/admin/auth';import {homepageKeys,type HomepageHighlight,type HomepageReview,type HomepageSection} from '@/types/homepage-cms';
export interface PublicHomepageCms{sections:HomepageSection[];highlights:HomepageHighlight[];reviews:HomepageReview[];mediaUrls:Record<string,string>}
export async function getHomepageCms(preview=false):Promise<PublicHomepageCms|null>{const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return null;const admin=preview?await getAdminSession():null,db=admin?await createServerSupabaseClient():createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});if(admin){const{data:draft}=await db.from('homepage_drafts').select('payload').eq('property_id',admin.propertyId).maybeSingle();if(draft?.payload){const payload=draft.payload as {sections:HomepageSection[];highlights:HomepageHighlight[];reviews:HomepageReview[]},ids=payload.sections.flatMap(section=>section.backgroundMediaId?[section.backgroundMediaId]:[]),mediaResult=ids.length?await db.from('media_assets').select('id,file_path').in('id',ids):{data:[] as {id:string;file_path:string}[]};return{...payload,mediaUrls:Object.fromEntries((mediaResult.data||[]).map(item=>[item.id,`${url}/storage/v1/object/public/public-media/${item.file_path}`]))}}}const status=admin?undefined:'published';let sectionsQuery=db.from('homepage_sections').select('id,section_key,title,subtitle,body,eyebrow,cta_label,cta_link,background_media_id,settings,status,is_visible,sort_order').order('sort_order');if(status)sectionsQuery=sectionsQuery.eq('status',status).eq('is_visible',true);const{data:sections,error}=await sectionsQuery;if(error||!sections?.length)return null;const ids=sections.map(row=>row.id),mediaIds=sections.flatMap(row=>row.background_media_id?[row.background_media_id]:[]);const[links,highlights,reviews,media]=await Promise.all([db.from('homepage_section_links').select('section_id,entity_type,entity_id,sort_order').in('section_id',ids).order('sort_order'),db.from('homepage_highlights').select('id,title,description,icon_reference,status,is_visible,sort_order').in('status',admin?['draft','published']:['published']).order('sort_order'),db.from('reviews').select('id,author_name,origin,quote,status,is_visible,sort_order').eq('source_label','homepage_manual').in('status',admin?['draft','published']:['published']).order('sort_order'),mediaIds.length?db.from('media_assets').select('id,file_path').in('id',mediaIds):Promise.resolve({data:[] as {id:string;file_path:string}[]})]);const mapped=sections.map(row=>({id:row.id,key:row.section_key,title:row.title||'',subtitle:row.subtitle||'',body:row.body||'',eyebrow:row.eyebrow||'',ctaLabel:row.cta_label||'',ctaLink:row.cta_link||'',backgroundMediaId:row.background_media_id||'',settings:row.settings||{},status:row.status,visible:row.is_visible,sortOrder:row.sort_order,links:(links.data||[]).filter(item=>item.section_id===row.id).map(item=>({type:item.entity_type,id:item.entity_id,sortOrder:item.sort_order}))})) as HomepageSection[];return{sections:homepageKeys.map(key=>mapped.find(section=>section.key===key)).filter(Boolean) as HomepageSection[],highlights:(highlights.data||[]).filter(row=>admin||row.is_visible).map(row=>({id:row.id,title:row.title,description:row.description||'',icon:row.icon_reference||'',visible:row.is_visible,sortOrder:row.sort_order,status:row.status})),reviews:(reviews.data||[]).filter(row=>admin||row.is_visible).map(row=>({id:row.id,author:row.author_name||'',origin:row.origin||'',quote:row.quote,visible:row.is_visible,sortOrder:row.sort_order,status:row.status})),mediaUrls:Object.fromEntries((media.data||[]).map(row=>[row.id,`${url}/storage/v1/object/public/public-media/${row.file_path}`]))}}
