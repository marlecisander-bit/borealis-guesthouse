import { ChannelsManager } from '@/components/admin/ChannelsManager';
export const metadata={title:'Channel calendars | Borealis Admin'};
export default function ChannelsPage(){return <ChannelsManager/>}

