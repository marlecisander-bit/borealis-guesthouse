'use server';import {revalidatePath} from 'next/cache';import {requireAdmin} from '@/lib/admin/auth';import {adminSiteContentRepository} from '@/lib/repositories/admin/site-content';import type {SiteContentState,SiteDocument} from '@/types/site-content-cms';const text=(d:FormData,k:string)=>String(d.get(k)||'').trim();
export async function saveSiteDocument(key:SiteDocument['key'],fields:string[],_:SiteContentState,d:FormData):Promise<SiteContentState>{const session=await requireAdmin(['owner','manager','editor']),status=text(d,'status') as 'draft'|'published',data=Object.fromEntries(fields.map(field=>[field,text(d,field)]));if(!['draft','published'].includes(status))return{ok:false,message:'Choose draft or publish.'};if(key==='contact'&&data.email&&!/^\S+@\S+\.\S+$/.test(data.email))return{ok:false,message:'Enter a valid contact email.'};try{await adminSiteContentRepository.saveDocument(session,key,data,status);revalidatePath(`/admin/content/${key}`);revalidatePath('/about');revalidatePath('/contact');revalidatePath('/');return{ok:true,message:status==='published'?'Changes published.':'Draft saved.'}}catch(error){return{ok:false,message:error instanceof Error?error.message:'Could not save changes.'}}}
export async function saveNavigation(_:SiteContentState,d:FormData):Promise<SiteContentState>{const session=await requireAdmin(['owner','manager','editor']),count=Number(text(d,'count')),allowed=['/','/rooms','/experiences','/explore-koman','/transfers','/gallery','/about','/contact','/book'];const items=Array.from({length:count},(_,i)=>({id:'',label:text(d,`label_${i}`),href:text(d,`href_${i}`),location:(text(d,`location_${i}`)==='footer'?'footer':'header') as 'header'|'footer',visible:d.get(`visible_${i}`)==='on',sortOrder:Number(text(d,`order_${i}`)||i*10),status:'published' as const})).filter(item=>item.label&&item.href);if(items.some(item=>!allowed.includes(item.href)&&!/^https:\/\/[a-z0-9.-]+(?:\/[^\s]*)?$/i.test(item.href)))return{ok:false,message:'Use an approved internal destination or a valid HTTPS URL.'};try{await adminSiteContentRepository.saveNavigation(session,items);revalidatePath('/');revalidatePath('/admin/content/navigation');return{ok:true,message:'Navigation published.'}}catch(error){return{ok:false,message:error instanceof Error?error.message:'Navigation could not be saved.'}}}
