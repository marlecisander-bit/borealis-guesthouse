import Link from'next/link';import{AdminEmptyState,AdminPageHeader}from'@/components/admin/ui';import{SiteDocumentForm}from'@/components/admin/SiteDocumentForm';import{requireAdmin}from'@/lib/admin/auth';import{adminSiteContentRepository}from'@/lib/repositories/admin/site-content';
const fields=[{name:'heroTitle',label:'Page title'},{name:'introduction',label:'Introduction',kind:'textarea' as const},{name:'heroImage',label:'Hero image URL',kind:'url' as const},{name:'storyHeading',label:'Story heading'},{name:'storyText',label:'Story content',kind:'textarea' as const},{name:'storyImage',label:'Story image URL',kind:'url' as const},{name:'locationHeading',label:'Location heading'},{name:'locationText',label:'Location content',kind:'textarea' as const},{name:'locationImage',label:'Location image URL',kind:'url' as const},{name:'ctaLabel',label:'CTA label'},{name:'ctaTarget',label:'CTA destination'}];
export default async function Page(){const session=await requireAdmin(['owner','manager','editor']);const doc=await adminSiteContentRepository.document(session,'about').catch(()=>null);if(!doc)return <AdminEmptyState title="CMS database update required" description="Run database/migrations/20260902_007_site_content_cms.sql in Supabase, then reload."/>;return <div className="space-y-8"><AdminPageHeader title="About page" description="Edit the approved story and location sections while the public layout remains protected." actions={<Link href="/about" target="_blank" className="rounded-lg border border-slate-300 bg-white px-4 py-3 text-sm font-semibold">Preview Website</Link>}/><SiteDocumentForm documentKey="about" fields={fields} data={doc.data}/></div>}
