import {MediaLibrary} from '@/components/admin/MediaLibrary';import{requireAdmin}from '@/lib/admin/auth';import{adminMediaRepository}from '@/lib/repositories/admin/media';
export default async function MediaPage(){const session=await requireAdmin(['owner','manager','editor']);const assets=await adminMediaRepository.list(session);return <div className="space-y-8"><header><h1 className="text-3xl font-bold text-slate-900">Media Library</h1><p className="mt-1 text-slate-600">Upload once, then reuse images throughout the Borealis website.</p></header><MediaLibrary initialAssets={assets}/></div>}
