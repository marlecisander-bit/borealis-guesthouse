import {MediaLibrary} from '@/components/admin/MediaLibrary';import{AdminPageHeader}from '@/components/admin/ui';import{requireAdmin}from '@/lib/admin/auth';import{adminMediaRepository}from '@/lib/repositories/admin/media';
export default async function MediaPage(){const session=await requireAdmin(['owner','manager','editor']);const assets=await adminMediaRepository.list(session);return <div className="space-y-8"><AdminPageHeader title="Media library" description="Upload once, then reuse images throughout the Borealis website."/><MediaLibrary initialAssets={assets}/></div>}
