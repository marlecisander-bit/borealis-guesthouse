'use client';
import { useActionState } from 'react';
import { saveHomepage } from '@/app/admin/content/actions';
import type { AdminActionState, CmsPage } from '@/types/admin';
const initialState:AdminActionState={ok:false,message:''};
export function CmsPageForm({page}:{page:CmsPage|null}){const[state,action,pending]=useActionState(saveHomepage,initialState);const field='mt-2 min-h-12 w-full rounded-lg border border-slate-300 bg-white px-4 text-slate-900 focus:border-slate-900';return <form action={action} className="grid gap-6"><section className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold text-slate-950">Homepage introduction</h2><label className="text-sm font-semibold text-slate-800">Internal page title<input name="title" defaultValue={page?.title||'Homepage'} className={field} aria-describedby={state.errors?.title?'title-error':undefined}/>{state.errors?.title&&<span id="title-error" className="mt-1 block text-xs text-red-700">{state.errors.title}</span>}</label><label className="text-sm font-semibold text-slate-800">Eyebrow<input name="eyebrow" defaultValue={page?.content.eyebrow||'Borealis Guest House · Koman, Albania'} className={field}/></label><label className="text-sm font-semibold text-slate-800">Main heading<input name="heroHeading" defaultValue={page?.content.heroHeading||'Wake up by the water.'} className={field}/>{state.errors?.heroHeading&&<span className="mt-1 block text-xs text-red-700">{state.errors.heroHeading}</span>}</label><label className="text-sm font-semibold text-slate-800">Supporting text<textarea name="heroCopy" defaultValue={page?.content.heroCopy||'A lakeside stay in the heart of Koman.'} rows={3} className={`${field} py-3`}/>{state.errors?.heroCopy&&<span className="mt-1 block text-xs text-red-700">{state.errors.heroCopy}</span>}</label></section><section className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold text-slate-950">Search appearance</h2><label className="text-sm font-semibold text-slate-800">SEO title<input name="seoTitle" defaultValue={page?.seoTitle||''} maxLength={60} className={field}/></label><label className="text-sm font-semibold text-slate-800">SEO description<textarea name="seoDescription" defaultValue={page?.seoDescription||''} maxLength={160} rows={3} className={`${field} py-3`}/></label></section><div className="sticky bottom-3 flex flex-col gap-3 rounded-xl border border-slate-200 bg-white/95 p-4 shadow-xl backdrop-blur sm:flex-row sm:items-center sm:justify-between"><p role="status" className={`text-sm ${state.ok?'text-emerald-700':'text-red-700'}`}>{state.message}</p><div className="flex gap-2"><button name="status" value="draft" disabled={pending} className="min-h-11 rounded-lg border border-slate-300 px-4 text-sm font-semibold">Save draft</button><button name="status" value="published" disabled={pending} className="min-h-11 rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white">{pending?'Saving…':'Save & publish'}</button></div></div></form>}
