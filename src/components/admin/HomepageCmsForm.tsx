'use client';import {useActionState} from 'react';import {saveHomepageCms} from '@/app/admin/content/homepage/actions';import type {CmsOption,HomepageCmsState,HomepageEditorData,HomepageKey,HomepageSection} from '@/types/homepage-cms';
const initial:HomepageCmsState={ok:false,message:''};const input='mt-2 min-h-11 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm';const names:Record<HomepageKey,string>={hero:'Hero',property_highlights:'Property highlights',intro:'Introduction',featured_rooms:'Featured rooms',featured_experiences:'Experiences',explore_koman:'Explore Koman',transfers:'Transfers',gallery:'Gallery preview',reviews:'Reviews',location:'Location',final_cta:'Final booking CTA'};
export function HomepageCmsForm({data}:{data:HomepageEditorData}){const[state,action,pending]=useActionState(saveHomepageCms,initial);const highlights=[...data.highlights,...Array.from({length:3},()=>({id:'',title:'',description:'',icon:'',visible:true,sortOrder:0,status:'draft' as const}))],reviews=[...data.reviews,...Array.from({length:2},()=>({id:'',author:'',origin:'',quote:'',visible:true,sortOrder:0,status:'draft' as const}))];return <form action={action} className="grid gap-5"><nav className="flex gap-2 overflow-x-auto rounded-xl border border-slate-200 bg-white p-2">{data.sections.map(section=><a key={section.key} href={`#${section.key}`} className="shrink-0 rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-slate-100">{names[section.key]}</a>)}</nav>{data.sections.map(section=><SectionEditor key={section.key} section={section} data={data}/>) }<section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><h2 className="text-xl font-bold">Highlight entries</h2><p className="mt-1 text-sm text-slate-500">Blank rows are ignored. Use sort order to arrange entries.</p><input type="hidden" name="highlightCount" value={highlights.length}/><div className="mt-5 grid gap-4">{highlights.map((item,i)=><div key={`${item.id}-${i}`} className="grid gap-3 rounded-xl bg-slate-50 p-4 sm:grid-cols-2 lg:grid-cols-[1fr_1.5fr_1fr_90px_auto]"><Field label="Title"><input name={`highlight_${i}_title`} defaultValue={item.title} className={input}/></Field><Field label="Short description"><input name={`highlight_${i}_description`} defaultValue={item.description} className={input}/></Field><Field label="Icon identifier"><input name={`highlight_${i}_icon`} defaultValue={item.icon} className={input}/></Field><Field label="Order"><input name={`highlight_${i}_order`} type="number" min="0" defaultValue={item.sortOrder||i*10} className={input}/></Field><label className="flex items-end gap-2 pb-3 text-sm font-semibold"><input name={`highlight_${i}_visible`} type="checkbox" defaultChecked={item.visible}/>Enabled</label></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><h2 className="text-xl font-bold">Manual testimonials</h2><p className="mt-1 text-sm text-slate-500">These are manual entries and are not presented as synchronized reviews.</p><input type="hidden" name="reviewCount" value={reviews.length}/><div className="mt-5 grid gap-4">{reviews.map((item,i)=><div key={`${item.id}-${i}`} className="grid gap-3 rounded-xl bg-slate-50 p-4 sm:grid-cols-2"><Field label="Guest name"><input name={`review_${i}_author`} defaultValue={item.author} className={input}/></Field><Field label="Origin"><input name={`review_${i}_origin`} defaultValue={item.origin} className={input}/></Field><Field label="Testimonial"><textarea name={`review_${i}_quote`} defaultValue={item.quote} rows={3} className={`${input} py-3`}/></Field><div className="grid grid-cols-2 gap-3"><Field label="Order"><input name={`review_${i}_order`} type="number" min="0" defaultValue={item.sortOrder||i*10} className={input}/></Field><label className="flex items-end gap-2 pb-3 text-sm font-semibold"><input name={`review_${i}_visible`} type="checkbox" defaultChecked={item.visible}/>Show</label></div></div>)}</div></section><div className="sticky bottom-3 z-20 flex flex-col gap-3 rounded-xl border border-slate-200 bg-white/95 p-4 shadow-xl backdrop-blur sm:flex-row sm:items-center sm:justify-between"><p role="status" className={`text-sm ${state.ok?'text-emerald-700':'text-red-700'}`}>{state.message}</p><div className="flex gap-2"><button name="status" value="draft" disabled={pending} className="min-h-11 rounded-lg border border-slate-300 px-4 text-sm font-semibold">Save Draft</button><button name="status" value="published" disabled={pending} className="min-h-11 rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white">Publish</button></div></div></form>}
function SectionEditor({section,data}:{section:HomepageSection;data:HomepageEditorData}){const options=section.key==='featured_rooms'?data.rooms:section.key==='featured_experiences'?data.experiences:section.key==='explore_koman'?data.articles:section.key==='transfers'?data.transfers:section.key==='gallery'?data.media:[];const selected=new Map(section.links.map(link=>[link.id,link.sortOrder]));return <section id={section.key} className="scroll-mt-6 rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Approved section</p><h2 className="mt-1 text-xl font-bold">{names[section.key]}</h2></div><div className="flex gap-4"><label className="text-sm font-semibold">Order <input name={`${section.key}_order`} type="number" min="0" defaultValue={section.sortOrder} className="ml-2 w-20 rounded-lg border border-slate-300 p-2"/></label><label className="flex items-center gap-2 text-sm font-semibold"><input name={`${section.key}_visible`} type="checkbox" defaultChecked={section.visible}/>Show</label></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label={section.key==='hero'?'Main headline':'Heading'}><input name={`${section.key}_title`} defaultValue={section.title} className={input}/></Field><Field label="Eyebrow (optional)"><input name={`${section.key}_eyebrow`} defaultValue={section.eyebrow} className={input}/></Field><Field label="Subtitle"><input name={`${section.key}_subtitle`} defaultValue={section.subtitle} className={input}/></Field><Field label="CTA label"><input name={`${section.key}_ctaLabel`} defaultValue={section.ctaLabel} className={input}/></Field><Field label="Text"><textarea name={`${section.key}_body`} defaultValue={section.body} rows={3} className={`${input} py-3`}/></Field><Field label="CTA destination"><input name={`${section.key}_ctaLink`} defaultValue={section.ctaLink} placeholder="/rooms" className={input}/></Field>{['hero','intro','final_cta'].includes(section.key)&&<Field label="Image"><select name={`${section.key}_media`} defaultValue={section.backgroundMediaId} className={input}><option value="">Use current fallback image</option>{data.media.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select></Field>}{section.key==='hero'&&<label className="flex items-end gap-2 pb-3 text-sm font-semibold"><input name="hero_showBookingSearch" type="checkbox" defaultChecked={section.settings.showBookingSearch!==false}/>Show booking search</label>}{section.key==='location'&&<Field label="Map link/reference"><input name="location_mapLink" defaultValue={String(section.settings.mapLink||'')} placeholder="https://maps.google.com/..." className={input}/></Field>}</div>{options.length>0&&<OptionSelection name={`${section.key}_links`} options={options} selected={selected}/>}</section>}
function OptionSelection({name,options,selected}:{name:string;options:CmsOption[];selected:Map<string,number>}){return <fieldset className="mt-5"><legend className="text-sm font-semibold">Featured content</legend><p className="mt-1 text-xs text-slate-500">Select records and use order to arrange them. Their details stay managed in their own modules.</p><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{options.map((option,index)=><div key={option.id} className="flex min-h-11 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm"><label className="flex flex-1 items-center gap-2"><input name={name} type="checkbox" value={option.id} defaultChecked={selected.has(option.id)}/>{option.label}</label><input aria-label={`${option.label} order`} name={`${name.slice(0,-5)}link_order_${option.id}`} type="number" min="0" defaultValue={selected.get(option.id)??index*10} className="w-16 rounded border border-slate-300 p-1"/></div>)}</div></fieldset>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="text-sm font-semibold text-slate-800">{label}{children}</label>}
