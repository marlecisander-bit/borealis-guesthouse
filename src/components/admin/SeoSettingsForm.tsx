'use client';import{useActionState,useState}from'react';import{saveGlobalSeo}from'@/app/admin/seo/actions';import type{GlobalSeoSettings}from'@/types/seo-admin';const initial={ok:false,message:''},input='mt-2 min-h-12 w-full rounded-lg border border-slate-300 bg-white px-4';export function SeoSettingsForm({settings,media}:{settings:GlobalSeoSettings;media:{id:string;label:string}[]}){const[state,action,pending]=useActionState(saveGlobalSeo,initial),[title,setTitle]=useState(settings.defaultTitle),[description,setDescription]=useState(settings.defaultDescription);return <form action={action} className="grid gap-6"><section className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">Global SEO settings</h2><div className="grid gap-5 md:grid-cols-2"><Field label="Default site title" count={title.length} recommended="Recommended: 30–60 characters"><input name="defaultTitle" value={title} onChange={e=>setTitle(e.target.value)} className={input}/></Field><Field label="Title template"><input name="titleTemplate" defaultValue={settings.titleTemplate} placeholder="%s | Borealis Guest House" className={input}/></Field><Field label="Site name"><input name="siteName" defaultValue={settings.siteName} className={input}/></Field><Field label="Default OG image"><select name="defaultOgMediaId" defaultValue={settings.defaultOgMediaId} className={input}><option value="">No default image</option>{media.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></Field></div><Field label="Default meta description" count={description.length} recommended="Recommended: 120–160 characters"><textarea name="defaultDescription" value={description} onChange={e=>setDescription(e.target.value)} rows={3} className={`${input} py-3`}/></Field><div className="grid gap-3 sm:grid-cols-2"><Check name="robotsIndex" label="Index pages by default" checked={settings.robotsIndex}/><Check name="robotsFollow" label="Follow links by default" checked={settings.robotsFollow}/></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">Search-result preview</h2><div className="mt-5 max-w-2xl rounded-xl border border-slate-200 p-5 font-sans"><p className="truncate text-sm text-emerald-800">{typeof window==='undefined'?'https://borealis.example':window.location.origin.replace('/admin/seo','')}</p><p className="mt-1 text-xl text-[#1a0dab]">{title||'Untitled page'}</p><p className="mt-1 text-sm leading-6 text-slate-600">{description||'Add a default meta description to preview how the site may appear in search results.'}</p></div><p className="mt-3 text-xs text-slate-500">Search engines may rewrite titles and descriptions. This preview is indicative only.</p></section><div className="sticky bottom-3 flex items-center justify-between rounded-xl border border-slate-200 bg-white/95 p-4 shadow-xl backdrop-blur"><p role="status" className={state.ok?'text-sm text-emerald-700':'text-sm text-red-700'}>{state.message}</p><button disabled={pending} className="min-h-11 rounded-lg bg-slate-950 px-5 text-sm font-bold text-white">{pending?'Saving…':'Save global SEO'}</button></div></form>}
function Field({label,count,recommended,children}:{label:string;count?:number;recommended?:string;children:React.ReactNode}){return <label className="block text-sm font-semibold">{label}{children}{count!==undefined&&<span className={`mt-1 block text-xs ${count>160?'text-amber-700':'text-slate-500'}`}>{count} characters · {recommended}</span>}</label>}function Check({name,label,checked}:{name:string;label:string;checked:boolean}){return <label className="flex min-h-12 items-center gap-3 rounded-lg border border-slate-200 px-4 font-semibold"><input name={name} type="checkbox" defaultChecked={checked}/>{label}</label>}
