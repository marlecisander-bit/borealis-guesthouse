'use client';import{useActionState,useState}from'react';import{saveNavigation}from'@/app/admin/content/site-actions';import type{NavigationItem}from'@/types/site-content-cms';
const destinations=['/','/rooms','/experiences','/explore-koman','/transfers','/gallery','/about','/contact','/book'];
export function NavigationForm({initial}:{initial:NavigationItem[]}){const[items,setItems]=useState(initial.length?initial:[{id:'',label:'Rooms',href:'/rooms',location:'header',visible:true,sortOrder:10,status:'published'}]);const[state,action,pending]=useActionState(saveNavigation,{ok:false,message:''});return <form action={action} className="space-y-5"><input type="hidden" name="count" value={items.length}/>{items.map((item,i)=><div key={`${item.id}-${i}`} className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 md:grid-cols-[1fr_1.4fr_.7fr_.5fr_auto]"><label className="text-xs font-bold text-slate-600">Label<input name={`label_${i}`} defaultValue={item.label} className="mt-2 min-h-11 w-full rounded-lg border border-slate-300 px-3 text-sm font-normal"/></label><label className="text-xs font-bold text-slate-600">Destination<input name={`href_${i}`} defaultValue={item.href} list="approved-destinations" className="mt-2 min-h-11 w-full rounded-lg border border-slate-300 px-3 text-sm font-normal"/></label><label className="text-xs font-bold text-slate-600">Location<select name={`location_${i}`} defaultValue={item.location} className="mt-2 min-h-11 w-full rounded-lg border border-slate-300 px-3 text-sm font-normal"><option value="header">Header</option><option value="footer">Footer</option></select></label><label className="text-xs font-bold text-slate-600">Order<input name={`order_${i}`} type="number" defaultValue={item.sortOrder} className="mt-2 min-h-11 w-full rounded-lg border border-slate-300 px-3 text-sm font-normal"/></label><div className="flex items-end gap-3"><label className="flex min-h-11 items-center gap-2 text-sm"><input name={`visible_${i}`} type="checkbox" defaultChecked={item.visible}/> Enabled</label><button type="button" onClick={()=>setItems(v=>v.filter((_,x)=>x!==i))} className="min-h-11 text-sm font-semibold text-red-700">Remove</button></div></div>)}<datalist id="approved-destinations">{destinations.map(x=><option key={x} value={x}/>)}</datalist><button type="button" onClick={()=>setItems(v=>[...v,{id:'',label:'',href:'/',location:'header',visible:true,sortOrder:v.length*10+10,status:'published'}])} className="min-h-11 rounded-lg border border-slate-300 px-4 font-semibold">Add link</button>{state.message&&<p role="status" className="text-sm">{state.message}</p>}<div className="flex justify-end"><button disabled={pending} className="min-h-12 rounded-lg bg-slate-950 px-6 font-semibold text-white">{pending?'Saving…':'Save navigation'}</button></div></form>}
