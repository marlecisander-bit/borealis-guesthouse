'use client';
export function ConfirmAction({action,label,confirmMessage}:{action:()=>Promise<void>;label:string;confirmMessage:string}){return <form action={action} onSubmit={(event)=>{if(!window.confirm(confirmMessage))event.preventDefault()}}><button className="inline-flex min-h-11 items-center justify-center rounded-lg border border-red-200 bg-white px-4 text-center text-sm font-semibold leading-none text-red-700">{label}</button></form>}
