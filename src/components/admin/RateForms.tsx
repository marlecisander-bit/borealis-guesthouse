'use client';
import {useActionState} from 'react';import {saveBaseRate,savePricingRule,saveSeasonalRate} from '@/app/admin/rates/actions';import {pricingRuleTypes,type RateActionState,type RateRoomType,type SeasonalRate} from '@/types/rates-admin';
const initial:RateActionState={ok:false,message:''};const input='mt-2 min-h-11 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm';
export function BaseRateForm({room}:{room:RateRoomType}){const[state,action,pending]=useActionState(saveBaseRate,initial);return <form action={action} className="grid gap-3 rounded-xl bg-slate-50 p-4 sm:grid-cols-[minmax(0,1fr)_130px_90px_auto] sm:items-end"><input type="hidden" name="roomTypeId" value={room.id}/><div><p className="font-semibold text-slate-900">{room.name}</p><p className="text-xs text-slate-500">Standard nightly price</p></div><Field label="Nightly rate"><input name="price" required type="number" min="0" step="0.01" defaultValue={room.basePrice??''} className={input}/></Field><Field label="Currency"><input name="currency" required maxLength={3} defaultValue={room.currency} className={`${input} uppercase`}/></Field><button disabled={pending} className="min-h-11 rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white">Save</button>{state.message&&<p role="status" className={`text-xs sm:col-span-4 ${state.ok?'text-emerald-700':'text-red-700'}`}>{state.message}</p>}</form>}
export function SeasonalRateForm({rooms,period}:{rooms:RateRoomType[];period:SeasonalRate|null}){const[state,action,pending]=useActionState(saveSeasonalRate,initial);return <form action={action} className="grid gap-4"><input type="hidden" name="id" value={period?.id||''}/><Field label="Period name"><input name="name" required defaultValue={period?.name||''} placeholder="e.g. High season" className={input}/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Start date"><input name="startDate" required type="date" defaultValue={period?.startDate||''} className={input}/></Field><Field label="End date"><input name="endDate" required type="date" defaultValue={period?.endDate||''} className={input}/></Field><Field label="Nightly price"><input name="price" required type="number" min="0" step="0.01" defaultValue={period?.price??''} className={input}/></Field><Field label="Currency"><input name="currency" required maxLength={3} defaultValue={period?.currency||'EUR'} className={`${input} uppercase`}/></Field></div><fieldset><legend className="text-sm font-semibold text-slate-800">Applicable room types</legend><div className="mt-2 grid gap-2 sm:grid-cols-2">{rooms.map(room=><label key={room.id} className="flex min-h-11 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm"><input name="roomTypeIds" type="checkbox" value={room.id} defaultChecked={period?.roomTypeIds.includes(room.id)}/>{room.name}</label>)}</div>{state.errors?.roomTypeIds&&<p className="mt-1 text-xs text-red-700">{state.errors.roomTypeIds}</p>}</fieldset><label className="flex items-center gap-2 text-sm font-semibold"><input name="active" type="checkbox" defaultChecked={period?.active??true}/>Active</label><p role="status" className={`text-sm ${state.ok?'text-emerald-700':'text-red-700'}`}>{state.message}</p><button disabled={pending} className="min-h-11 rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white">{period?'Update period':'Add seasonal rate'}</button></form>}
export function PricingRuleForm({rooms}:{rooms:RateRoomType[]}){const[state,action,pending]=useActionState(savePricingRule,initial);return <form action={action} className="grid gap-4"><Field label="Rule name"><input name="name" required placeholder="e.g. Weekend minimum stay" className={input}/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Rule type"><select name="type" className={input}>{pricingRuleTypes.map(type=><option key={type} value={type}>{type.replaceAll('_',' ')}</option>)}</select></Field><Field label="Room type"><select name="roomTypeId" className={input}><option value="">All room types</option>{rooms.map(room=><option key={room.id} value={room.id}>{room.name}</option>)}</select></Field><Field label="Start date (optional)"><input name="startDate" type="date" className={input}/></Field><Field label="End date (optional)"><input name="endDate" type="date" className={input}/></Field><Field label="Value"><input name="value" type="number" min="0" step="0.01" placeholder="Not needed for closures" className={input}/></Field></div><label className="flex items-center gap-2 text-sm font-semibold"><input name="active" type="checkbox" defaultChecked/>Active</label><p role="status" className={`text-sm ${state.ok?'text-emerald-700':'text-red-700'}`}>{state.message}</p><button disabled={pending} className="min-h-11 rounded-lg border border-slate-300 bg-white px-4 text-sm font-semibold">Add pricing rule</button></form>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="text-sm font-semibold text-slate-800">{label}{children}</label>}
