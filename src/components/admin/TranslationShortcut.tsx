'use client';import Link from'next/link';import{usePathname}from'next/navigation';export function TranslationShortcut(){const pathname=usePathname(),match=pathname.match(/^\/admin\/(rooms|experiences|transfers|explore-koman)\/([^/]+)$/),content=/^\/admin\/content\/(homepage|about|contact|navigation)$/.test(pathname);if(content)return <Link href="/admin/languages" className="fixed bottom-5 right-5 z-40 rounded-full bg-indigo-700 px-5 py-3 text-sm font-bold text-white shadow-xl">Translations</Link>;if(!match||match[2]==='new')return null;const types:Record<string,string>={rooms:'room_type',experiences:'experience',transfers:'transfer_route','explore-koman':'tourism_article'},entity=`${types[match[1]]}:${match[2]}`;return <Link href={`/admin/languages?entity=${encodeURIComponent(entity)}`} className="fixed bottom-5 right-5 z-40 rounded-full bg-indigo-700 px-5 py-3 text-sm font-bold text-white shadow-xl">Translations</Link>}
