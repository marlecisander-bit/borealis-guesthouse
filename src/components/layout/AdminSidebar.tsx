'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { adminNavigation } from '@/lib/admin/navigation';
export default function AdminSidebar({onNavigate}:{onNavigate?:()=>void}){const pathname=usePathname();const groups=[...new Set(adminNavigation.map(item=>item.group))];return <aside className="flex h-full w-64 shrink-0 flex-col border-r border-slate-200 bg-white"><div className="border-b border-slate-200 px-6 py-5"><Link href="/admin/dashboard" onClick={onNavigate} className="text-2xl font-bold text-slate-950">Borealis</Link><p className="mt-1 text-xs font-medium uppercase tracking-widest text-slate-500">Property CMS</p></div><nav className="flex-1 overflow-y-auto p-4" aria-label="Admin navigation">{groups.map(group=><div key={group} className="mb-5"><p className="mb-2 px-3 text-[.65rem] font-bold uppercase tracking-[.16em] text-slate-400">{group}</p><div className="space-y-1">{adminNavigation.filter(item=>item.group===group).map(item=>{const active=pathname===item.href||pathname.startsWith(`${item.href}/`);return <Link key={item.href} href={item.href} onClick={onNavigate} className={`block rounded-lg px-3 py-2.5 text-sm font-semibold transition ${active?'bg-slate-950 text-white':'text-slate-700 hover:bg-slate-100'}`}>{item.label}</Link>})}</div></div>)}</nav><div className="border-t border-slate-200 p-4"><Link href="/" className="block rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-slate-100">View public website</Link></div></aside>}
