import Link from 'next/link';
import {formatRoomRate} from '@/lib/pricing/format';
export function RoomBookingBar({slug,price,currency}:{slug:string;price:number|null;currency:string}){return <div className="safe-bottom-bar fixed inset-x-0 bottom-0 z-30 border-t border-lake/10 bg-white/95 px-4 pt-3 shadow-[0_-8px_30px_rgba(23,50,77,.12)] backdrop-blur md:hidden"><div className="mx-auto flex max-w-lg items-center gap-4"><p className="text-xs text-muted">{price===null?'Pricing':'From'}<br/><strong className="text-lg text-lake">{formatRoomRate(price,currency)}</strong>{price!==null&&' / night'}</p><Link href={`/book?room=${slug}`} className="ml-auto min-h-14 flex-1 rounded-full bg-lake px-5 py-4 text-center text-sm font-bold text-white">Check availability</Link></div></div>}
