import Link from 'next/link';
import type { ContactInfo } from '@/types/public';
export function MapPlaceholder({ contact }: { contact: ContactInfo }) { return <div className="relative min-h-[28rem] overflow-hidden rounded-[1.75rem] bg-green text-white"><div className="absolute inset-5 rounded-[1.25rem] border border-white/20 bg-[radial-gradient(circle_at_65%_30%,rgba(232,222,208,.35),transparent_22%),linear-gradient(135deg,rgba(255,255,255,.08),transparent)]"/><div className="absolute inset-0 grid place-items-center p-8 text-center"><div><span className="mx-auto grid size-14 place-items-center rounded-full bg-sand font-serif text-2xl text-lake">B</span><h2 className="mt-4 font-serif text-3xl">Borealis · Koman</h2><p className="mx-auto mt-2 max-w-xs text-sm leading-6 text-white/65">{contact.address}</p>{contact.mapsUrl?<Link href={contact.mapsUrl} target="_blank" className="mt-6 inline-block rounded-full bg-white px-6 py-3 text-sm font-bold text-lake">Open Google Maps</Link>:<p className="mt-5 text-xs font-bold uppercase tracking-widest text-sand">Map link to be connected</p>}</div></div></div> }
